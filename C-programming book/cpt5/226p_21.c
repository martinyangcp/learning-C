/*

#include <stdio.h>
int main() {
	int b, i, a;

	printf("두개의 정수 입력 :");
	scanf("%d %d", &a, &b);
	printf("%d이지만 %d의 배수는 아닌 수", a, b);
	for (i = 1; i < 101; i++) {
		
		if ((i % a) == 0 && (i % b) != 0) {
			printf("%d ", i);
		}

	}



}
*/