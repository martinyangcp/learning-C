/*


#include <stdio.h>
int main() {
	
	char mun, start;
	int i,startt ;

	printf("문자 입력 :");
	scanf("%c", &mun);
	i = (int)mun;
	for (start = 'a'; start < 'z'; start++) {
		if (start == mun) {
			printf("알파벳소문자 입니다.");
		}
	}
	for (start = 'A'; start < 'Z'; start++) {
		if (start == mun) {
			printf("알파벳대문자 입니다.");
		}
	}
	for  (startt = 0 ; startt < 10; startt++) {
		if (startt == i) {
			printf("숫자 입니다.");
		}
	}


}
*/