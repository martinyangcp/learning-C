/*

#include <stdio.h>
int main() {

	int num,fir,sec,trd,fth;
	printf("네자리 정수를 입력하세요 :");
	scanf("%d", &num);
	fir = num / 1000;
	sec = (num % 1000) / 100;
	trd = (num % 100)/10;
	fth = num % 10;
	printf("입력한 수는 %d천%d백%d십%d",fir,sec,trd,fth);

}
*/