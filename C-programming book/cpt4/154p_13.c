/*

#include <stdio.h>
int main() {
	int fir, sec, thd,bigger,biggest;

	printf("세 개의 정수 : ");
	scanf("%d %d %d", &fir, &sec, &thd);
	
	bigger = (fir >= sec) ? fir : sec;
	biggest = (bigger >= thd) ? bigger : thd;
	printf("가장 큰 수 =%d", biggest);


}
*/