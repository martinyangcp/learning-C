/*
#include <stdio.h>
#define SQ(x) ((x)*(x))
int main() {
	int n = 0, i = 0,sum=0;

	printf("n의 값을 입력하세요 :");
	scanf("%d", &n);
	for (i = 1; i < n+1; i++) {
		printf("%d+", SQ(i));
		sum += SQ(i);
	}
	printf("=%d", sum);

}
*/