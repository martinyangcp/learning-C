/*

#include <stdio.h>

int compa(int A, int B);			// A,B 중 큰 값을 반환함

int main() {
	int fir, sec;

	printf("첫째 정수?");
	scanf("%d", &fir);
	printf("둘째 정수?");
	scanf("%d", &sec);
	printf("%d, %d 중 큰 값은%d",fir,sec, compa(fir,sec));

}

int compa(int A, int B) {
	
	if (A > B) {
		return A;
	}
	else {
		return B;
	}
}

*/