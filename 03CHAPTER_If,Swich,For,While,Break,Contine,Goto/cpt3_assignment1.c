/*

#include <stdio.h>

int main() {
	int num ,odd=0, even=0,k;

	printf("정수 n 을 입력하시오 :");
	scanf("%d", &num);

	for (k = 1; k <= num; k += 2) {
		odd += k;
	}
	for (k = 0; k <= num; k += 2) {
		even += k;
	}

	printf("정수 1에서 %d이하 홀수들의 합은 %d이고 짝수들의 합은 %d입니다.", num, odd, even);














}
*/